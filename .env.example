# ========================
# Configuración de Entorno
# ========================
#
# INSTRUCCIONES:
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Reemplaza los valores de ejemplo con tus credenciales reales
# 3. NUNCA subas el archivo .env a Git (ya está en .gitignore)
#

# --- Estado del Entorno ---
# Valores: development | production
# En development se muestran errores PHP en pantalla
APP_ENV=development

# --- Configuración del Proyecto ---
# Nombre usado para contenedores Docker y prefijos
PROJECT_NAME=project_name

# --- Configuración de la Base de Datos ---
# Host: usar "project_name_db" para Docker, "localhost" para desarrollo local
DB_HOST=project_name_db
DB_NAME=project_name_db
DB_USER=tu_usuario_db
DB_PASS=tu_password_db
DB_ROOT_PASS=tu_password_root
DB_CHARSET=utf8mb4
DB_SQL_MODE="NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION"
DB_GROUPCONCAT_MAX_LEN=1000000

# --- Configuración del Servidor de Correo ---
# Configuración SMTP para envío de emails (recuperación de contraseña, etc.)
MAIL_HOST=smtp.tu-servidor.com
MAIL_USER=tu-email@dominio.com
MAIL_PASS=tu_password_smtp
MAIL_PORT=465
MAIL_REPLYTO_EMAIL=tu-email@dominio.com
MAIL_FROM_NAME="Nombre de tu Proyecto"
MAIL_CHARSET=utf-8
MAIL_LANGUAGE=es
